# Zig build artifacts and fetched dependency sources
.zig-cache/
zig-cache/
zig-out/
zig-pkg/

# Generated sprite sheets + previews (ADR 0031): DERIVED from committed `.zon`
# recipes via `mise run assets`; the recipe is the source of truth, never the sheet.
**/sprites/generated/

# Editor / OS noise
*.swp
.DS_Store

# Personal, per-machine Claude Code settings (settings.json IS committed)
.claude/settings.local.json

# Transient subagent git worktrees
.claude/worktrees/

# Orchestration runbook + live loop state (always local)
LOOP.md
